# folk-playground
Trying out some folk computer programs

Check out `folkville` dir for an example app and the virtual testing harness `browser-table` dir for those without a complete folk setup at home
